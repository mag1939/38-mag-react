1. create each components 
2. set basic structure Layout.jsx, Navbar.jsx, Home.jsx and Owner.jsx\
3. working on "react-router-dom"
4. install react-router-dom
5. createBrowserRouter structure on App.jsx and call it from RouterProvider
6. Create component reuseable "ViewToggleButton" and use destructure to get paremeters when import for using (we need "onClick action" and "name" for each button)
7. Set basic structure on Home-UserSection and Home-AdminSection
8. Navigate to "Home"
9. import "Button" and "setStage" when clicking it
10. write a JS condition to check your "setState-Variable" if `{a === "b" && do-something}` to show each "Home-View" their contents
11. 